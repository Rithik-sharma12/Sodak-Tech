-r base.txt

# Testing
pytest==9.0.1
pytest-django==4.12.0
pytest-cov==7.0.0

# Factories for test data. Preferred over fixtures so tests stay readable
# as the schema grows.
factory-boy==3.3.3

# Lint and format
ruff==0.15.1

# Type checking. django-stubs tracks Django's own version, so this pin moves
# with the Django pin in base.txt.
mypy==1.19.0
django-stubs[compatible-mypy]==6.0.7
djangorestframework-stubs==3.17.1

# Note on query-count assertions: SODAK-TECH-STACK.md §3.3 requires CI to fail
# when an N+1 regression lands on a hot endpoint. django-test-plus was the
# obvious dependency for that but caps at Python <3.14. Django's built-in
# assertNumQueries covers the same ground with no dependency at all -- see
# apps/common/tests/test_query_counts.py for the harness.
